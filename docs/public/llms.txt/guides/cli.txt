# JSSON Documentation — LLM Mode
Source: cli.mdx
Version: 0.0.5.2
Generated: 2025-12-10T14:15:49.348Z

## Table of Contents
- 0.1. Basic Usage
- 0.2. Command-Line Flags
- 0.2.1. Input/Output
- 1. PRINT TO TERMINAL
- 2. SAVE TO FILE
- 3. PIPE TO ANOTHER COMMAND
- 3.0.1. Format Options
- 4. JSON (DEFAULT)
- 5. YAML
- 6. TOML
- 7. TYPESCRIPT
- 7.0.1. Help & Version
- 8. SHOW HELP
- 9. SHOW VERSION
- 9.0.1. Streaming Options
- 10. ENABLE STREAMING EXPLICITLY
- 11. AUTO-ENABLE FOR RANGES > 5000 ITEMS
- 12. DEFAULT: AUTO-ENABLES FOR RANGES > 10,000 ITEMS
- 12.1. Common Workflows
- 12.1.1. 1. Quick Transpilation
- 12.1.2. 2. Check Output Without Saving
- 12.1.3. 3. Pipe to Other Tools
- 13. USE WITH JQ
- 14. COUNT LINES
- 15. SEARCH FOR A VALUE
- 15.0.1. 4. Batch Processing
- 16. CONVERT ALL .JSSON FILES IN A DIRECTORY
- 17. CONVERT ALL .JSSON FILES
- 17.0.1. 5. Watch for Changes
- 18. SIMPLE WATCH LOOP
- 19. AUTO-TRANSPILE ON FILE CHANGE
- 19.1. Integration Examples
- 19.1.1. NPM Scripts
- 19.1.2. Makefile
- 20. MAKEFILE
- 20.0.1. GitHub Actions
- 20.0.2. Docker
- 20.1. Error Handling
- 20.1.1. Exit Codes
- 20.1.2. Capturing Errors
- 20.2. Tips & Tricks
- 20.2.1. 1. Validate Without Output
- 21. JUST CHECK IF JSSON IS VALID
- 21.0.1. 2. Compare Before/After
- 22. SEE WHAT CHANGED
- 22.0.1. 3. Format Existing JSON
- 23. RE-FORMAT JSON USING JSSON
- 24. (NOTE: JSSON DOESN'T PARSE JSON, BUT YOU CAN USE JQ FOR THIS)
- 24.0.1. 4. Environment-Specific Configs
- 25. DEVELOPMENT
- 26. PRODUCTION
- 26.0.1. 5. Pre-commit Hook
- 27. TRANSPILE JSSON FILES BEFORE COMMIT
- 27.1. Troubleshooting
- 27.1.1. Command Not Found
- 28. USE FULL PATH
- 29. OR ADD TO PATH
- 29.0.1. Permission Denied
- 29.0.2. File Not Found
- 29.1. What's Next?• [Getting Started](/guides/getting-started/) — New to JSSON? Start here• [Syntax Reference](/reference/syntax/) — Complete syntax guide• [Errors & Debugging](/reference/errors/) — Troubleshooting help

## Content

The JSSON CLI is your gateway to transpiling JSSON files into JSON, YAML, TOML, or TypeScript. Let's master it! 🚀

0.1. Basic Usage

The simplest way to use JSSON:

``bash
jsson -i input.jsson > output.json
`

This reads input.jsson and writes the transpiled JSON to output.json using shell redirection.

0.2. Command-Line Flags

0.2.1. Input/Output

 •  Flag        •  Short  •  Description                              •  Example            • 
 •  ----------  •  -----  •  ---------------------------------------  •  -----------------  • 
 •  --input   •  -i   •  Input JSSON file (required)              •  -i config.jsson  • 
 •  --format  •  -f   •  Output format (optional, default: json)  •  -f yaml          • 

Note: The CLI always prints to stdout (terminal). Use > to save to a file.

`bash

1. PRINT TO TERMINAL
-----------------
jsson -i config.jsson


2. SAVE TO FILE
------------
jsson -i config.jsson > config.json


3. PIPE TO ANOTHER COMMAND
-----------------------
jsson -i config.jsson  •  jq '.app.name'
`

3.0.1. Format Options

 •  Flag        •  Description                                            •  Example          • 
 •  ----------  •  -----------------------------------------------------  •  ---------------  • 
 •  --format  •  Output format: json (default), yaml, toml, ts  •  --format yaml  • 

`bash

4. JSON (DEFAULT)
--------------
jsson -i config.jsson > config.json


5. YAML
----
jsson -i config.jsson -f yaml > config.yaml


6. TOML
----
jsson -i config.jsson -f toml > config.toml


7. TYPESCRIPT
----------
jsson -i config.jsson -f ts > config.ts
`

7.0.1. Help & Version

`bash

8. SHOW HELP
---------
jsson --help
jsson -h


9. SHOW VERSION
------------
jsson --version
jsson -v
`

9.0.1. Streaming Options

For large datasets, JSSON supports streaming mode to reduce memory usage:

 •  Flag                  •  Description                                 •  Default  • 
 •  --------------------  •  ------------------------------------------  •  -------  • 
 •  --stream            •  Enable streaming mode                       •  false  • 
 •  --stream-threshold  •  Auto-enable streaming for ranges > N items  •  10000  • 

`bash

10. ENABLE STREAMING EXPLICITLY
---------------------------
jsson -i large-data.jsson --stream > output.json


11. AUTO-ENABLE FOR RANGES > 5000 ITEMS
-----------------------------------
jsson -i data.jsson --stream-threshold 5000 > output.json


12. DEFAULT: AUTO-ENABLES FOR RANGES > 10,000 ITEMS
-----------------------------------------------
jsson -i data.jsson > output.json
`

When to use streaming:• Generating 100k+ items• Large matrix/grid data (100x100+)• Memory-constrained environments• Processing millions of records

Performance:• ✅ 100k items: ~30MB output, < 50MB memory• ✅ 1M items: Processable without OOM• ✅ Nested transformations: Optimized

12.1. Common Workflows

12.1.1. 1. Quick Transpilation

`bash
jsson -i config.jsson > config.json
`

12.1.2. 2. Check Output Without Saving

`bash
jsson -i config.jsson
`

Perfect for testing your JSSON before committing!

12.1.3. 3. Pipe to Other Tools

`bash

13. USE WITH JQ
-----------
jsson -i config.jsson  •  jq '.database.host'


14. COUNT LINES
-----------
jsson -i config.jsson  •  wc -l


15. SEARCH FOR A VALUE
------------------
jsson -i config.jsson  •  grep "localhost"
`

15.0.1. 4. Batch Processing

PowerShell:

`powershell

16. CONVERT ALL .JSSON FILES IN A DIRECTORY
---------------------------------------
Get-ChildItem .jsson  •  ForEach-Object {
    $output = $_.BaseName + ".json"
    jsson -i $_.Name > $output
}
`

Bash:

`bash

17. CONVERT ALL .JSSON FILES
------------------------
for file in .jsson; do
    jsson -i "$file" > "${file%.jsson}.json"
done
`

17.0.1. 5. Watch for Changes

PowerShell:

`powershell

18. SIMPLE WATCH LOOP
-----------------
while ($true) {
    jsson -i config.jsson > config.json
    Start-Sleep -Seconds 2
}
`

Bash with entr:

`bash

19. AUTO-TRANSPILE ON FILE CHANGE
-----------------------------
ls *.jsson  •  entr -s 'jsson -i config.jsson > config.json'
`

19.1. Integration Examples

19.1.1. NPM Scripts

package.json:

`json
{
  "scripts": {
    "build:config": "jsson -i config.jsson > dist/config.json",
    "watch:config": "nodemon --watch config.jsson --exec 'npm run build:config'"
  }
}
`

19.1.2. Makefile

`makefile

20. MAKEFILE
--------
.PHONY: build clean

build: config.json

config.json: config.jsson
	jsson -i config.jsson > config.json

clean:
	rm -f config.json
`

20.0.1. GitHub Actions

.github/workflows/build.yml:

`yaml
name: Build Config

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:• uses: actions/checkout@v2• name: Download JSSON
        run:  • 
          wget https://github.com/carlosedujs/jsson/releases/latest/download/jsson-linux
          chmod +x jsson-linux• name: Transpile JSSON
        run: ./jsson-linux -i config.jsson > config.json• name: Upload artifact
        uses: actions/upload-artifact@v2
        with:
          name: config
          path: config.json
`

20.0.2. Docker

Dockerfile:

`dockerfile
FROM golang:1.21-alpine AS builder

WORKDIR /build
COPY . .
RUN go build -o jsson cmd/jsson/main.go

FROM alpine:latest
COPY --from=builder /build/jsson /usr/local/bin/
COPY config.jsson /app/

WORKDIR /app
RUN jsson -i config.jsson > config.json

CMD ["cat", "config.json"]
`

20.1. Error Handling

20.1.1. Exit Codes

 •  Code  •  Meaning                                 • 
 •  ----  •  --------------------------------------  • 
 •  0   •  Success                                 • 
 •  1   •  Error (lex, parse, or transpile error)  • 
 •  2   •  Invalid arguments                       • 

Example:

`bash
jsson -i config.jsson > config.json
if [ $? -eq 0 ]; then
    echo "Success!"
else
    echo "Error transpiling JSSON"
    exit 1
fi
`

20.1.2. Capturing Errors

PowerShell:

`powershell
try {
    jsson -i config.jsson > config.json
    if ($LASTEXITCODE -ne 0) {
        throw "JSSON transpilation failed"
    }
} catch {
    Write-Error $_
    exit 1
}
`

Bash:

`bash
if ! jsson -i config.jsson > config.json 2> errors.log; then
    echo "Error: $(cat errors.log)"
    exit 1
fi
`

20.2. Tips & Tricks

20.2.1. 1. Validate Without Output

`bash

21. JUST CHECK IF JSSON IS VALID
----------------------------
jsson -i config.jsson > /dev/null
echo $?  # 0 = valid, 1 = error
`

21.0.1. 2. Compare Before/After

`bash

22. SEE WHAT CHANGED
----------------
jsson -i old.jsson > old.json
jsson -i new.jsson > new.json
diff old.json new.json
`

22.0.1. 3. Format Existing JSON

`bash

23. RE-FORMAT JSON USING JSSON
--------------------------

24. (NOTE: JSSON DOESN'T PARSE JSON, BUT YOU CAN USE JQ FOR THIS)
-------------------------------------------------------------
cat config.json  •  jq .
`

24.0.1. 4. Environment-Specific Configs

`bash

25. DEVELOPMENT
-----------
jsson -i config/dev.jsson > dist/config.json


26. PRODUCTION
----------
jsson -i config/prod.jsson > dist/config.json
`

26.0.1. 5. Pre-commit Hook

.git/hooks/pre-commit:

`bash
#!/bin/bash

27. TRANSPILE JSSON FILES BEFORE COMMIT
-----------------------------------

for file in $(git diff --cached --name-only  •  grep '\.jsson$'); do
    output="${file%.jsson}.json"
    jsson -i "$file" > "$output"
    git add "$output"
done
`

27.1. Troubleshooting

27.1.1. Command Not Found

`
bash: jsson: command not found
`

Solution: Add JSSON to your PATH or use the full path:

`bash

28. USE FULL PATH
-------------
/path/to/jsson -i config.jsson


29. OR ADD TO PATH
--------------
export PATH=$PATH:/path/to/jsson
`

29.0.1. Permission Denied

`
Permission denied: jsson
`

Solution: Make it executable:

`bash
chmod +x jsson
`

29.0.2. File Not Found

`
Error: cannot read file: config.jsson
`

Solution: Check the file exists and path is correct:

`bash
ls -la config.jsson  # Verify file exists
jsson -i ./config.jsson  # Try with ./
``

29.1. What's Next?• [Getting Started](/guides/getting-started/) — New to JSSON? Start here• [Syntax Reference](/reference/syntax/) — Complete syntax guide• [Errors & Debugging](/reference/errors/) — Troubleshooting help

Master the CLI and integrate JSSON into your workflow! 🎯